
#
# VARIABLE is VALUE
# This is a token pattern. Try not to use them. But they are simpler for copulative verbs ("be" verbs)
#
- name: ${label}-cop-1
  priority: ${rulepriority}
  label: ${label}
  type: token
  pattern: |
    @variable:Variable [tag=/VBN/]? (?<trigger> [lemma=be]) @value:Value

#
# [With] Variable Value
# This is a token pattern.
#
- name: ${label}-cop-2
  priority: ${rulepriority}
  label: ${label}
  type: token
  pattern: |
    (?<trigger> [lemma=with])? @variable:Variable (@value:Value)+

#
# VARIABLE ranged|occurred from|between VALUE and VALUE
# This is a syntactic/semantic pattern. Try to use such rules when you can. They generalize better!
#
- name: ${label}-range-1
  priority: ${rulepriority}
  label: ${label}
  graph: "hybrid"
  pattern: |
    trigger = [lemma=/range|occur/ & tag=/^V/]
    variable:Variable = /nsubj/
    value:Value = /nmod_from|nmod_between|Ax_from|Ax_between/

