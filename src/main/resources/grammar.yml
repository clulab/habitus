rules:
    - name: example-rule
      type: event
      label: EatingEvent
      priority: 1
      pattern: |
          trigger = [lemma=eat]
          theme: ^food = >dobj
          tool: ^tool = >nmod_with >conj?
